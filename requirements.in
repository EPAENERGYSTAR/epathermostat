# Pinned to a commit, not to @feature/power-source: a branch tip moves, and
# requirements.txt already resolved this exact commit -- the two could
# silently install different eeweather.
eeweather @ git+https://github.com/opendsm/eeweather.git@854480763a7467ccce00fd42f9919b17c17b55cf
numpy>=2,<3
pandas>=2.2,<3
scipy>=1.11
python-dateutil
pytz
